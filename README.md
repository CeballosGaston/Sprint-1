## 📖 Descripción

Este repositorio contiene un **proyecto frontend** implementado con **tres enfoques de CSS**:

1. **Vanilla CSS** – HTML y CSS puro.
2. **SASS** – Preprocesador para modularización y optimización de estilos.
3. **Bootstrap** – Framework CSS para componentes y diseño responsivo rápido.



---

## 🌿 Ramas del proyecto

#Rama - feature/vanilla-implementation (Base sin frameworks).
- Tecnologías usadas: HTML + CSS

#Rama - feature/SASS-implementation (Variables, mixins, anidamiento, modularización, CSS optimizado).
-Tecnologías usadas: HTML + CSS + SASS

#Rama - feature/bootstrap-implementation (Diseño responsivo, clases y componentes predefinidos, desarrollo rápido).
-Tecnologías usadas: Bootstrap 5 + HTML + CSS

> 🔹 Cada rama es independiente y contiene un enfoque completo del proyecto.

---

## 🚀 Instalación y uso

1. **Clonar el repositorio**
```bash
git clone (https://github.com/CeballosGaston/Sprint-1.git)

Cambiar a la rama deseada:
git checkout feature/vanilla-implementation
git checkout feature/SASS-implementation
git checkout feature/bootstrap-implementation

Abrir el proyecto

Abre index.html en tu navegador.

Para la rama sass, compila los archivos .scss:

bash
Copiar código
sass sass/main.scss:css/main.css


📂 Estructura de carpetas SASS

Copiar código
/
├── css/
│   ├── main.css
│   └── main.css.map
├── sass/
│   ├── layout.scss
│   ├── mediaquerys.scss
│   ├── mixins.scss 
│   ├── placeholders.scss 
│   ├── variables.scss 
│   └── main.scss
│      
└── index.html
└── .gitignore
└── index.js
└── Readme


🛠 Tecnologías
HTML5, CSS3

SASS (rama sass)

Bootstrap 5 (rama bootstrap)

Git & GitHub
